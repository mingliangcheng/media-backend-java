server:
  port: 8080
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://122.51.230.162:3306/lease
    username: root
    password: c2011020@
  data:
    redis:
      database: 0
      port: 6379
      host: 122.51.230.162
      password: c2011020@
      timeout: 10000

mybatis-plus:
  mapper-locations: classpath:/mapper/*.xml
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
    map-underscore-to-camel-case: true
  global-config:
    db-config:
      id-type: auto

minio:
  endpoint: http://122.51.230.162:9000
  accessKey: 0wYzOSjLKxikkE9CHAW5
  secretKey: nsicCdSazmmO2nOZedpnPFdCq7uz1XDfWqCzxHDx
  bucketName: test

logging:
  level:
    root: INFO # 设置全局日志级别
    org.springframework.web: DEBUG # 设置特定包的日志级别
  file:
    name: logs/app.log # 日志文件路径和名称
  pattern:
    file: "%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n" # 文件日志格式
    console: "%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n" # 控制台日志格式